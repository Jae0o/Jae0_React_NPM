import { css } from "styled-components";

export const RESET_BUTTON = css`
  background: inherit;
  border: none;
  box-shadow: none;
  border-radius: 0;
  padding: 0;
  overflow: visible;
  cursor: pointe;
`;
